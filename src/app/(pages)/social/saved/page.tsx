import React from "react";

type Props = {};

const SocialSaved = (props: Props) => {
  return (
    <div className="min-h-screen">
      <h2 className="h3-bold md:h2-bold w-full">Saved Content</h2>
      <p>Currently we are still in development</p>
    </div>
  );
};
export default SocialSaved;
